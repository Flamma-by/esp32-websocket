set(
    SRCS 	
    "websocket.c" 
    "websocket_server.c"
    )
set(
    INCLUDE_DIRS 
    "./include"
    )
set(
    REQUIRES
    lwip 
    mbedtls
    )
idf_component_register(
    SRCS
        ${SRCS}
    INCLUDE_DIRS
        ${INCLUDE_DIRS}
    REQUIRES
        ${REQUIRES}
)